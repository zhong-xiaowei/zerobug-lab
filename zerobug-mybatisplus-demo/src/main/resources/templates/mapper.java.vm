package ${package.Service};

import ${superServiceClassPackage};
import ${package.Entity}.${entity};

/**
 * $!{table.comment} Mapper 接口
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${table.mapperName} extends MapperPro<${entity}> {

}
